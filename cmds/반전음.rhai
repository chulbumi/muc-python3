// 반전음.rhai - 반전음 (음양 전환)
// Python: cmds/반전음.py

fn cmd(ob, line) {
    send_line(ob, "☞ 음양 전환 기능은 현재 구현 중입니다.");
    send_line(ob, "   (음양 공부 시 음/양성 전환)");
}

fn main(ob, line) {
    cmd(ob, line);
}
