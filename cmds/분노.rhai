// 분노.rhai - 분노 시스템
// Python: cmds/분노.py

fn cmd(ob, line) {
    send_line(ob, "☞ 분노 시스템은 현재 구현 중입니다.");
    send_line(ob, "   (전투 중 분노도 100 이상시 특수 스킬 사용)");
}

fn main(ob, line) {
    cmd(ob, line);
}
